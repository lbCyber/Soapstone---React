import React from 'react';
import firebase from './firebase.js';

const msgWindow = (props) => {
  <form action="" id="messageWindowForm">
    <select name="structureChoice" id="structureChoice">

    </select>
  </form>
}

export default messageWindow;